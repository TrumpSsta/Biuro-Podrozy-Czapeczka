@model IEnumerable<BiuroPodrozyCzapeczka.Models.Pracownik>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Imie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nazwisko)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.telefon)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pensja)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Placowka.Wojewodztwo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Imie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwisko)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.telefon)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pensja)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Placowka.Wojewodztwo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPracownika }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPracownika }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPracownika })
        </td>
    </tr>
}

</table>
