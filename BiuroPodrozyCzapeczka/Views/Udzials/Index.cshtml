@model IEnumerable<BiuroPodrozyCzapeczka.Models.Udzial>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IloscOsob)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Wplacone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Klient.Imię)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Wycieczka.Panstwo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IloscOsob)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Wplacone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Klient.Imię)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Wycieczka.Panstwo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdUdzialu }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdUdzialu }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdUdzialu })
        </td>
    </tr>
}

</table>
